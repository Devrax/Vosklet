{
  "name": "vosklet-demos",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "android:sync": "pnpm run build && pnpm exec cap sync android",
    "android:open": "pnpm run android:sync && pnpm exec cap open android",
    "android:run": "pnpm run android:sync && pnpm exec cap run android",
    "ios:sync": "pnpm run build && pnpm exec cap sync ios",
    "ios:open": "pnpm run ios:sync && pnpm exec cap open ios",
    "ios:run": "pnpm run ios:sync && pnpm exec cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest",
    "@capacitor/ios": "^8.4.2",
    "onnxruntime-web": "1.16.3",
    "vosklet-mono": "file:../../vosklet-mono/vosklet-mono-1.0.0.tgz",
    "vosklet-speaker": "file:../../vosklet-speaker/vosklet-speaker-1.0.0.tgz"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "vite": "latest"
  }
}